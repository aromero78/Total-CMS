using System;
using System.IO;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace TotalTech.CMS.Misc {
    public class gZip {
        public static string Compress(string ValueToCompress){
            /*string fileToBeCompressed = "D:My Great Word Document.doc";
            string zipFilename =
                "D:CompressedGreatDocument.zip";

            using (FileStream target =
                new FileStream(zipFilename,
                    FileMode.Create, FileAccess.Write))
            using (GZipStream alg =
                new GZipStream(target, CompressionMode.Compress)) {
                byte[] data = File.ReadAllBytes(fileToBeCompressed);
                alg.Write(data, 0, data.Length);
                alg.Flush();
            }*/
        }
    }
}
